# PAG protocol modules

Protocol modules terminate, validate, enforce policy for, and proxy application protocols.

Initial planned modules: `pag-dns`, `pag-http`, `pag-smtp`, `pag-ftp`, and `pag-irc`.

M0 intentionally does not freeze an RPC ABI. The lifecycle and policy interface will be prototyped in M1 before compatibility guarantees are made.
